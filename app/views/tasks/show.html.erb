<h2><%= @tasks.title %></h2>
<h2><%= @tasks.details %></h2>
<h2><%= @tasks.completed %></h2>

<%= link_to "Delete #{@tasks.title}",
            task_path(@tasks),
            method: :delete,
            data: { confirm: "Are you sure?" } %>

<%= link_to 'Back', index_path %>
